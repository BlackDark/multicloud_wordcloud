import BaseElement from "js/visualization/elements/BaseElement";

export default class NormalLink extends BaseElement{
	constructor(id) {
		super(id);
	}

	draw(container) {
		super.draw(container);
	}
}
