

export default class NodeSorter {
	constructor(nodes, endPoints) {
		this._nodes = nodes;
		this._endPoints = endPoints;
		this._skippedNodes = [];
		this._placedNodes = [];
	}

	hasNodes() {
		// TODO
		return false;
	}

	getNodeForEndPoints(endpoint) {
		// TODO does not remove from array
		return null;
	}

	placeNode(node) {

	}

	skipNode(node) {

	}

	// Iterate through the endpoint and give node back
	getNextNode() {

	}
}
